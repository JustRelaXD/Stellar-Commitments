[package]
name = "stellar-vault"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.7.7" }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
